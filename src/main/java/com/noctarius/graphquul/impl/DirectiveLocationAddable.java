package com.noctarius.graphquul.impl;

import com.noctarius.graphquul.ast.DirectiveLocation;

interface DirectiveLocationAddable {

    void addDirectiveLocation(DirectiveLocation directiveLocation);
}
